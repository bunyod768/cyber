pip install uvicorn,fastapi

start -> uvicorn [your_fl_nm] : app --reload

if you use virtual machine,you have to use this code -> uvicorn main:app --reload --host 0.0.0.0

run app -> python3 main.py

in browser: [ip addr]:[using port] /docs 
 or 
[ip addr]:[using port]/redoc